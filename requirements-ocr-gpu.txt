# GPU PaddleOCR (PP-OCRv4 / PP-OCRv5) — for CUDA GPU hosts (RunPod, your 2nd GPU box).
#
# IMPORTANT: paddlepaddle-gpu must match the host CUDA version, so it is NOT pinned
# here — install it FIRST from Paddle's CUDA index, then this file. Examples:
#
#   # CUDA 11.8
#   pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
#   # CUDA 12.6
#   pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#
# Check the host CUDA with `nvidia-smi` (top-right "CUDA Version").
# Do NOT install both paddlepaddle and paddlepaddle-gpu — pick one.
# After installing paddlepaddle-gpu, set OCR_USE_GPU=true in .env.

# paddleocr 3.x is required for PP-OCRv5 (2.9.x silently falls back to PP-OCRv4).
paddleocr>=3.0,<3.2
opencv-python-headless==4.10.0.84
pillow==11.0.0
shapely==2.0.6
pyclipper==1.3.0.post6
