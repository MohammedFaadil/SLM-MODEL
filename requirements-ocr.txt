# PaddleOCR (PP-OCRv4 / PP-OCRv5) — CPU build.
# Install AFTER requirements.txt:  pip install -r requirements-ocr.txt
#
# NOTE (Windows / CPU): paddlepaddle ships CPU wheels for Python 3.8-3.11.
# If pip cannot find a wheel, use the official index:
#   pip install paddlepaddle==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
# For an NVIDIA GPU box later, install the matching paddlepaddle-gpu build instead.

paddlepaddle==3.0.0
paddleocr==2.9.1

# PaddleOCR pulls these, pinned here for reproducibility.
opencv-python-headless==4.10.0.84
pillow==11.0.0
shapely==2.0.6
pyclipper==1.3.0.post6
